# Copy to .env for local development.

# VITE_API_BASE_URL=http://localhost:5101
VITE_API_BASE_URL=https://customer-api.wishdem.com

# Google sign-in isn't wired to a real OAuth client yet — the button stays
# hidden everywhere (login page, embedded auth prompts) until this is "true".
VITE_ENABLE_GOOGLE_AUTH=false
